version: '2'
services:
    masternodesonlinesupplement-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb masternodesonlinesupplement-mongodb-config
    masternodesonlinesupplement-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    masternodesonlinesupplement-mongodb-config:
        image: mongo:3.4.10
        container_name: masternodesonlinesupplement-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
